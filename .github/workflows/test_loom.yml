name: Test with Loom

permissions:
  contents: read

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always
  SCCACHE_GHA_ENABLED: on

jobs:
  test:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest

    steps: 
    - name: Setup sccache
      uses: mozilla-actions/sccache-action@v0.0.9

    - name: Install stable rust
      uses: dtolnay/rust-toolchain@stable
      
    - name: Install nextest
      uses: taiki-e/install-action@nextest
      
    - name: Checkout the repository
      uses: actions/checkout@v6
      with:
        submodules: true

    - name: Run Loom tests
      run: cargo nextest run --no-fail-fast --release -- --include-ignored loom
      env:
        RUST_BACKTRACE: full
        RUST_LOG: debug
        RUSTC_WRAPPER: sccache
        RUSTFLAGS: --cfg loom
